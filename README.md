# 🩸 Madara PM Security Bot

Protect your girlfriend's DM like Madara Uchiha 🔥

## ⚙️ Features
- Auto-warns & blocks unapproved PMs
- `/approve` `/block` `/listapproved` `/restart` commands
- Madara-style fonts & messages

## 🛠 Setup (Termux or Ubuntu VPS)
```bash
apt update && apt upgrade -y
apt install python3-pip -y
pip install -r requirements.txt
